const RANDOM_RUSH_DELAY=Number(process.env.RANDOM_RUSH_DELAY||20000);const http=require('node:http'),fs=require('node:fs'),path=require('node:path'),crypto=require('node:crypto'),{WebSocketServer}=require('ws');const{MODE_CONFIG,createLexicon,generateBoard,validateSubmission,normalizeWords}=require('./game-core');const PORT=Number(process.env.PORT||8000),HOST=process.env.HOST||'0.0.0.0',MAX_PLAYERS=10,rooms=new Map,clients=new Map;
function code(){const alphabet='ABCDEFGHJKLMNPQRSTUVWXYZ';let value;do value=Array.from({length:5},()=>alphabet[crypto.randomInt(alphabet.length)]).join('');while(rooms.has(value));return value}function send(ws,message){if(ws.readyState===1)ws.send(JSON.stringify(message))}function state(room){return{type:'room_state',code:room.code,creatorId:room.creatorId,randomRush:room.randomRush,mode:room.mode,status:room.status,players:[...room.players.values()].map(p=>({id:p.id,name:p.name,avatar:p.avatar||'🐈',score:p.score,connected:p.connected})),round:room.round?{board:room.round.board,size:room.round.size,endsAt:room.round.endsAt}:null}}function broadcast(room,message){for(const player of room.players.values())send(player.ws,message)}function randomMode(previous){const modes=['classic','minimum','sudden','race','coop','dirty'];const choices=modes.filter(mode=>mode!==previous);return choices[crypto.randomInt(choices.length)]}function startRound(room,selected=room.mode){room.mode=MODE_CONFIG[selected]?selected:'classic';const config=MODE_CONFIG[room.mode],board=generateBoard(config.size,createLexicon(room.mode,[...room.customWords]));room.round={board:board,size:config.size,found:new Set,endsAt:Date.now()+config.seconds*1000};room.status='playing';room.teamScore=0;for(const player of room.players.values())player.score=0;broadcast(room,{...state(room),type:'round_started',config})}function finishRound(room,reason='complete'){if(!room.round||room.status!=='playing')return;room.status='finished';broadcast(room,{type:'round_finished',cooperative:room.mode==='coop',teamScore:room.teamScore,stats:{wordsFound:room.round.found.size},reason,ranking:[...room.players.values()].sort((a,b)=>b.score-a.score).map(p=>({id:p.id,name:p.name,avatar:p.avatar||'🐈',score:p.score}))});if(room.randomRush)setTimeout(()=>{if(room.randomRush&&room.status==='finished')startRound(room,randomMode(room.mode))},RANDOM_RUSH_DELAY)}function leave(ws){const info=clients.get(ws);if(!info)return;clients.delete(ws);const room=rooms.get(info.roomCode);if(!room)return;if(info.id===room.creatorId){for(const member of room.players.values()){const memberClient=clients.get(member.id);if(memberClient)memberClient.roomCode=null;send(member.ws,{type:'session_closed',reason:'creator_disconnected'})}return rooms.delete(room.code)}room.players.delete(info.id);if(!room.players.size)return rooms.delete(room.code);broadcast(room,state(room))}
function handle(ws,message){const type=message?.type;if(type==='hello'){const id=String(message.guestId||crypto.randomUUID());clients.set(ws,{id,name:String(message.name||'Guest').slice(0,20),avatar:String(message.avatar||'🐈').slice(0,4),roomCode:null});return send(ws,{type:'hello_ack',id})}const client=clients.get(ws);if(!client)return send(ws,{type:'error',code:'HELLO_REQUIRED'});if(type==='create_room'){const mode=MODE_CONFIG[message.mode]?message.mode:'classic',room={code:code(),mode:'classic',creatorId:null,randomRush:false,teamScore:0,customWords:new Set(normalizeWords(message.customWords)),players:new Map,status:'lobby',round:null};rooms.set(room.code,room);client.roomCode=room.code;room.creatorId=client.id;client.name=String(message.name||client.name).slice(0,20);client.avatar=String(message.avatar||client.avatar||'🐈').slice(0,4);room.players.set(client.id,{...client,ws,score:0,connected:true});send(ws,{type:'room_created',code:room.code});broadcast(room,state(room));return}if(type==='join_room'){const room=rooms.get(String(message.code||'').toUpperCase());if(!room)return send(ws,{type:'error',code:'ROOM_NOT_FOUND'});if(room.players.size>=MAX_PLAYERS)return send(ws,{type:'error',code:'ROOM_FULL'});client.roomCode=room.code;client.name=String(message.name||client.name).slice(0,20);client.avatar=String(message.avatar||client.avatar||'🐈').slice(0,4);room.players.set(client.id,{...client,ws,score:0,connected:true});send(ws,{type:'joined_room',code:room.code});broadcast(room,state(room));return}const room=rooms.get(client.roomCode);if(!room)return send(ws,{type:'error',code:'NOT_IN_ROOM'});if(type==='update_identity'){client.name=String(message.name||client.name).slice(0,20);client.avatar=String(message.avatar||client.avatar||'🐈').slice(0,4);const identityRoom=rooms.get(client.roomCode);if(identityRoom&&identityRoom.players.has(client.id)){const player=identityRoom.players.get(client.id);player.name=client.name;player.avatar=client.avatar;broadcast(identityRoom,state(identityRoom))}return}if(type==='leave_session'){const leavingRoom=rooms.get(client.roomCode);if(!leavingRoom)return send(ws,{type:'error',code:'NOT_IN_ROOM'});if(client.id===leavingRoom.creatorId){for(const member of leavingRoom.players.values()){member.roomCode=null;send(member.ws,{type:'session_closed',reason:'creator_left'})}rooms.delete(leavingRoom.code)}else{leavingRoom.players.delete(client.id);client.roomCode=null;send(ws,{type:'session_left'});broadcast(leavingRoom,state(leavingRoom))}return}if(type==='start_game'){if(client.id!==room.creatorId)return send(ws,{type:'error',code:'CREATOR_ONLY'});if(room.status==='playing')return send(ws,{type:'error',code:'ROUND_PLAYING'});const requested=String(message.mode||'classic');if(requested==='random'){room.randomRush=true;return startRound(room,randomMode(room.mode))}room.randomRush=false;return startRound(room,MODE_CONFIG[requested]?requested:'classic');}if(type==='submit_word'){if(room.status!=='playing'||!room.round)return send(ws,{type:'error',code:'ROUND_NOT_PLAYING'});if(Date.now()>=room.round.endsAt)return finishRound(room,'timeout');const result=validateSubmission({...message,board:room.round.board,size:room.round.size,mode:room.mode,found:room.round.found,customWords:[...room.customWords]});if(!result.valid){send(ws,{type:'word_rejected',playerId:client.id,word:result.word,reason:result.reason});if(room.mode==='sudden'&&result.reason!=='duplicate')finishRound(room,'invalid_word');return}room.round.found.add(result.word);const player=room.players.get(client.id);if(room.mode==='coop'){room.teamScore+=result.points;for(const teammate of room.players.values())teammate.score=room.teamScore}else player.score+=result.points;broadcast(room,{type:'word_accepted',playerId:client.id,word:result.word,points:result.points,scores:[...room.players.values()].map(p=>({id:p.id,name:p.name,avatar:p.avatar||'🐈',score:p.score}))});if(room.mode==='race'&&player.score>=500)finishRound(room,'race');return}if(type==='end_round')finishRound(room,'manual')}
const server=http.createServer((req,res)=>{if((req.url||'').split('?')[0]==='/dictionary.json'){res.writeHead(200,{'Content-Type':'application/json','Cache-Control':'public, max-age=86400'});return res.end(JSON.stringify(createLexicon('classic')))}let requested=decodeURIComponent((req.url||'/').split('?')[0]);if(requested==='/')requested='/index.html';const file=path.join(__dirname,requested);if(!file.startsWith(__dirname)||!fs.existsSync(file)||fs.statSync(file).isDirectory()){res.writeHead(404);return res.end('Not found')}const ext=path.extname(file),types={'.html':'text/html','.js':'text/javascript','.css':'text/css','.png':'image/png'};res.writeHead(200,{'Content-Type':types[ext]||'application/octet-stream'});fs.createReadStream(file).pipe(res)});const wss=new WebSocketServer({server});wss.on('connection',ws=>{ws.on('message',raw=>{try{handle(ws,JSON.parse(raw))}catch{send(ws,{type:'error',code:'BAD_MESSAGE'})}});ws.on('close',()=>leave(ws))});if(require.main===module)server.listen(PORT,HOST,()=>console.log('Wordrush listening on http://'+HOST+':'+PORT));module.exports={server,rooms,handle,MAX_PLAYERS};


const { Leaderboard } = require('./leaderboard');
const leaderboard = new Leaderboard();
function readJson(req) { return new Promise(resolve => { let body = ''; req.on('data', chunk => { body += chunk; if (body.length > 10000) req.destroy(); }); req.on('end', () => { try { resolve(JSON.parse(body || '{}')); } catch { resolve(null); } }); }); }
async function leaderboardRequest(req, res) {
  const url = new URL(req.url, 'http://localhost');
  if (url.pathname === '/api/leaderboard' && req.method === 'GET') { res.writeHead(200, { 'Content-Type': 'application/json', 'Cache-Control': 'no-store' }); return res.end(JSON.stringify({ period: url.searchParams.get('period') === 'total' ? 'total' : 'weekly', players: leaderboard.rankings(url.searchParams.get('period')) })); }
  if (url.pathname.startsWith('/api/leaderboard/') && req.method === 'GET') { const player = leaderboard.profile(decodeURIComponent(url.pathname.slice('/api/leaderboard/'.length))); if (!player) { res.writeHead(404); return res.end(JSON.stringify({ error: 'PLAYER_NOT_FOUND' })); } res.writeHead(200, { 'Content-Type': 'application/json', 'Cache-Control': 'no-store' }); return res.end(JSON.stringify(player)); }
  if (url.pathname === '/api/leaderboard/score' && req.method === 'POST') { const body = await readJson(req); if (!body || !body.id) { res.writeHead(400, { 'Content-Type': 'application/json' }); return res.end(JSON.stringify({ error: 'INVALID_SCORE' })); } res.writeHead(200, { 'Content-Type': 'application/json' }); return res.end(JSON.stringify(leaderboard.recordScore(body))); }
  return false;
}
const originalRequestHandler = server.listeners('request')[0];
server.removeListener('request', originalRequestHandler);
server.on('request', async (req, res) => { if (res.writableEnded) return; const handled = await leaderboardRequest(req, res); if (!handled && !res.writableEnded) originalRequestHandler(req, res); });
