(() => {
  const guestId = localStorage.getItem('wordrush-guest-id') || crypto.randomUUID();
  localStorage.setItem('wordrush-guest-id', guestId);
  const profile = () => window.wordrushProfile ? window.wordrushProfile() : { name: 'Guest', avatar: '🐈' };
  const request = (url, options) => fetch(url, options).then(response => { if (!response.ok) throw new Error('leaderboard request failed'); return response.json(); });
  const scoreRound = score => { const value = Number(score) || 0; if (!value) return; const current = (() => { try { return JSON.parse(localStorage.getItem('wordrush-profile') || '{}'); } catch { return {}; } })(); const previous = (() => { try { return JSON.parse(sessionStorage.getItem('wordrush-leaderboard-baseline') || '{}'); } catch { return {}; } })(); const marker = [guestId, value, current.rounds || 0].join(':'); if (sessionStorage.getItem('wordrush-last-leaderboard-score') === marker) return; const delta = key => Math.max(0, (Number(current[key]) || 0) - (Number(previous[key]) || 0)); const multiplayer = window.wordrushLastMultiplayerResult; const own = multiplayer?.ranking?.find(player => player.id === guestId); const multiplayerWin = multiplayer ? Boolean(multiplayer.cooperative || (own && multiplayer.ranking[0]?.id === guestId)) : false; request('/api/leaderboard/score', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ id: guestId, ...profile(), score: value, words: delta('words'), correct: delta('correct'), incorrect: delta('incorrect'), longest: current.longest || 0, totalWordLength: delta('totalWordLength'), gameSeconds: Math.min(600, delta('totalGameSeconds')), multiplayer: Boolean(multiplayer), multiplayerWin }) }).then(() => { sessionStorage.setItem('wordrush-last-leaderboard-score', marker); sessionStorage.setItem('wordrush-leaderboard-baseline', JSON.stringify(current)); }).catch(() => {}); };
  function addUI() {
    const card = document.querySelector('#sessionCard'); if (!card || document.querySelector('#scoreboardButton')) return;
    const button = document.createElement('button'); button.id = 'scoreboardButton'; button.className = 'secondary scoreboard-button'; button.innerHTML = 'Global scoreboard <span>→</span>'; button.addEventListener('click', openBoard); card.after(button);
    const screen = document.createElement('section'); screen.className = 'screen'; screen.id = 'scoreboardScreen'; screen.innerHTML = '<div class="game-head"><button id="scoreboardBack">←</button><div><p class="eyebrow">Everyone is playing</p><h1>Scoreboard</h1></div></div><div class="scoreboard-tabs"><button class="active" data-period="weekly">This week</button><button data-period="total">All time</button><button data-period="multiplayer-wins">MP wins</button><button data-period="multiplayer-ratio">MP ratio</button></div><div id="scoreboardList" class="scoreboard-list"></div>';
    document.querySelector('#homeScreen').after(screen); screen.querySelector('#scoreboardBack').addEventListener('click', () => document.querySelector('[data-screen="homeScreen"]').click()); screen.querySelectorAll('[data-period]').forEach(tab => tab.addEventListener('click', () => { screen.querySelectorAll('[data-period]').forEach(x => x.classList.toggle('active', x === tab)); load(tab.dataset.period); }));
    const dialog = document.createElement('dialog'); dialog.id = 'leaderboardProfileDialog'; dialog.innerHTML = '<div class="dialog-head"><div><p class="eyebrow">Player profile</p><h2 id="leaderboardProfileName">Player</h2></div><button id="leaderboardProfileClose" aria-label="Close">×</button></div><div id="leaderboardProfileBody" class="leaderboard-profile"></div>'; document.body.append(dialog); dialog.querySelector('#leaderboardProfileClose').addEventListener('click', () => dialog.close());
  }
  function openBoard() { document.querySelectorAll('.screen').forEach(screen => screen.classList.toggle('active', screen.id === 'scoreboardScreen')); load('weekly'); }
  function load(period) { const list = document.querySelector('#scoreboardList'); if (!list) return; list.innerHTML = '<p class="scoreboard-empty">Loading the standings…</p>'; request('/api/leaderboard?period=' + period).then(data => { list.innerHTML = ''; if (!data.players.length) { list.innerHTML = '<p class="scoreboard-empty">No scores yet. Be the first legend.</p>'; return; } data.players.forEach((player, index) => { const row = document.createElement('button'); row.className = 'scoreboard-row'; row.innerHTML = '<span class="scoreboard-rank">' + (index + 1) + '</span><span class="scoreboard-avatar"></span><span class="scoreboard-player"></span><b></b>'; row.querySelector('.scoreboard-avatar').textContent = player.avatar; row.querySelector('.scoreboard-player').textContent = player.name; row.querySelector('b').textContent = period === 'multiplayer-ratio' ? (player.score * 100).toFixed(1) + '%' : player.score.toLocaleString(); row.addEventListener('click', () => showProfile(player.id)); list.append(row); }); }).catch(() => { list.innerHTML = '<p class="scoreboard-empty">Scoreboard unavailable right now.</p>'; }); }
  function showProfile(id) { request('/api/leaderboard/' + encodeURIComponent(id)).then(player => { const dialog = document.querySelector('#leaderboardProfileDialog'); document.querySelector('#leaderboardProfileName').textContent = player.avatar + ' ' + player.name; document.querySelector('#leaderboardProfileBody').innerHTML = '<div class="profile-score"><strong>' + player.totalScore.toLocaleString() + '</strong><small>total score</small></div><div class="profile-stat-grid"><span><b>' + player.rounds + '</b><small>rounds</small></span><span><b>' + player.totalWords + '</b><small>words</small></span><span><b>' + (player.totalWords ? (player.totalWordLength / player.totalWords).toFixed(1) : '0.0') + '</b><small>avg word length</small></span><span><b>' + (player.totalGameSeconds ? (player.totalWords / (player.totalGameSeconds / 60)).toFixed(1) : '0.0') + '</b><small>words / minute</small></span><span><b>' + player.multiplayerWins + '</b><small>multiplayer wins</small></span><span><b>' + (player.multiplayerWinRatio * 100).toFixed(1) + '%</b><small>multiplayer win ratio</small></span></div>'; dialog.showModal(); }).catch(() => {}); }
  const originalOnlineFinish = window.wordrushOnlineFinish; if (originalOnlineFinish) window.wordrushOnlineFinish = (ranking, result) => { window.wordrushLastMultiplayerResult = { ranking, cooperative: Boolean(result?.cooperative) }; const current = (() => { try { return JSON.parse(localStorage.getItem('wordrush-profile') || '{}'); } catch { return {}; } })(); current.multiplayerWins ||= 0; current.multiplayerLosses ||= 0; const own = ranking?.find(player => player.id === guestId); if (result?.cooperative || (own && ranking?.[0]?.id === guestId)) current.multiplayerWins += 1; else current.multiplayerLosses += 1; localStorage.setItem('wordrush-profile', JSON.stringify(current)); return originalOnlineFinish(ranking, result); };
  const observer = new MutationObserver(() => { if (document.querySelector('#resultsScreen')?.classList.contains('active')) scoreRound(document.querySelector('#finalScore')?.textContent); });
  addUI(); observer.observe(document.body, { subtree: true, attributes: true, attributeFilter: ['class'] });
})();
